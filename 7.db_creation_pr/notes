it`s time for assignment :)

Створіть програму на Python, яка працює з базою даних PostgreSQL.
Ваша програма повинна (1 -10 балів):
Підключатися до бази даних (параметри можна передати через словник, як у прикладі).
Створювати таблицю students, якщо вона ще не існує.
У таблиці мають бути такі поля:
id (SERIAL PRIMARY KEY)
first_name (TEXT NOT NULL)
last_name (TEXT NOT NULL)
grade (INTEGER NOT NULL)
Отримувати від користувача дані про студента (ім'я, прізвище, оцінка).
Вставляти введені дані у таблицю students.
Завдання на 11 та 12 :
Додати перевірку, щоб оцінка була числом від 1 до 12.
Додати можливість пошуку студента за прізвищем та виведення його оцінки.