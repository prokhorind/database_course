Створіть базу даних для компанії, де потрібно зберігати інформацію про співробітників та їхні паспортні дані.
Реалізуйте зв’язок "Один до одного" між таблицями employees та passports.

Створіть таблиці employees та passports, де:

employees має поля:
id (PRIMARY KEY)
name (ім'я)
position (посада)

passports має поля:
id (PRIMARY KEY)
employee_id (FOREIGN KEY на employees(id))
passport_number (номер паспорта, UNIQUE)
issued_by (ким виданий паспорт)
issued_year (рік видачі паспорта)
Використовуйте поведінку за замовченням для FOREIGN KEY (ON DELETE NO ACTION).

Вставте дані про трьох співробітників та їхні паспортні дані.

Виконайте запит на отримання повної інформації про співробітників разом із їхніми паспортними даними.







Сенс зв’язку "Один до одного" (1:1) у базах даних полягає в логічному розділенні даних для покращення структури, безпеки та продуктивності.

Ось основні причини, чому не варто просто додати паспортний номер у таблицю employees:

1️⃣ Розділення обов’язків (Normalization)
Якщо ми збережемо всі дані в одній таблиці, вона може стати занадто великою та містити багато NULL-значень, якщо не для всіх співробітників є паспортні дані.



в’язок "Один до одного" використовується, коли:
✅ Деякі дані рідко змінюються або є конфіденційними.
✅ Частина записів може не мати певної інформації (NULL у passport_number).
✅ Нам потрібно легко додавати або змінювати чутливі дані без впливу на основну таблицю.

Тому у випадку employees та passports логічно мати дві таблиці для гнучкості, безпеки та продуктивності. 🚀